# Copy this file to ".env" (same folder) and paste your key:
#   cp .env.example .env
# Then restart the backend.  Leave blank / unset to run in deterministic mode.

GEMINI_API_KEY=
# Optional: override the model (default: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash
